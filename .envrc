# If we are a computer with nix-shell available, then use that to setup
# the build environment with exactly what we need.
if has nix; then
  use flake
fi

# Read our .env after loading Nix
dotenv_if_exists
